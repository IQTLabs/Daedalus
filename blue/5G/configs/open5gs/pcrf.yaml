---
### For reference, see `nextepc.conf`

### haven't found a way to pass in user/pass yet
db_uri: mongodb://mongoadmin:secret@mongodb/open5gs?authSource=admin

logger:
  file: /usr/local/var/log/open5gs/pcrf.log
  trace:
    app: 1
    diameter: 1

parameter:
  no_ipv6: true

pcrf:
  freeDiameter: /usr/local/etc/freeDiameter/pcrf.conf
